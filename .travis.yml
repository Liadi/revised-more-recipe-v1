language: node_js

node_js:
- 'iojs'
- '6'

cache:
  directories:
    - 'node_modules'

services:
  - postgresql


before_script:
  - psql -c 'create database travis_ci_test;' -U postgres

test:
  adapter: postgresql
  database: travis_ci_test
